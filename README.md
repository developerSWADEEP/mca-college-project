# mca-college-project

Acon CMS (CodeIgniter) — local setup: see `LOCAL_SETUP.md`.

After clone, copy `cms/application/config/database.php.example` to `cms/application/config/database.php` and set your MySQL credentials.
